{
  "config": {
    "step": {
      "user": {
        "title": "Connect Ctronics IP Camera",
        "data": {
          "host": "IP address",
          "username": "Username",
          "password": "Password",
          "port": "Port",
          "alarm_folder": "Alarm folder (optional)"
        },
        "data_description": {
          "alarm_folder": "Folder this camera uploads its alarm snapshots to over FTP, e.g. /share/ctronics_hallway. Leave empty to skip person detection."
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid username or password",
      "cannot_connect": "Unable to reach the camera",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This camera is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "preset_count": "Number of PTZ preset buttons (max. 8)",
          "alarm_folder": "Alarm folder",
          "off_delay": "Keep detection on for (seconds)",
          "alarm_prefix": "Alarm file name starts with",
          "snapshot_folder": "Snapshot save location"
        },
        "data_description": {
          "alarm_folder": "The folder you set in the camera under FTP → path. Subfolders the camera creates inside it are searched automatically.",
          "alarm_prefix": "The camera uploads alarm images (A...), periodic snapshots (P...) and test images (T...) to the same folder. Only files starting with this letter count as a detection and get cleaned up. Empty = every image.",
          "snapshot_folder": "Folder the \"Save snapshot\" button writes to. Anywhere under /media can be viewed and downloaded straight from Home Assistant's Media panel."
        }
      }
    }
  },
  "entity": {
    "switch": {
      "ai_detection": {
        "name": "AI person detection"
      },
      "auto_tracking": {
        "name": "Auto-tracking"
      }
    },
    "number": {
      "detection_threshold": {
        "name": "Detection threshold"
      },
      "ircut_value": {
        "name": "IRCut switching time"
      }
    },
    "select": {
      "ir_mode": {
        "name": "IR LED control",
        "state": {
          "auto": "Auto",
          "open": "On",
          "close": "Off"
        }
      }
    },
    "button": {
      "preset": {
        "name": "Go to preset {number}"
      },
      "save_snapshot": {
        "name": "Save snapshot"
      }
    },
    "camera": {
      "snapshot": {
        "name": "Snapshot"
      }
    },
    "binary_sensor": {
      "person_detected": {
        "name": "Person detected"
      }
    },
    "image": {
      "last_detection": {
        "name": "Last detection"
      }
    }
  }
}
